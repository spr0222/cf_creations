name: Deploy VPC CloudFormation Stack

on:
  push:
    branches:
      - main
    paths:
      - infra/vpc-stack.yaml
      - .github/workflows/deploy-vpc-stack.yml
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Validate CloudFormation template
        run: aws cloudformation validate-template --template-body file://infra/vpc-stack.yaml

      - name: Deploy VPC CloudFormation stack
        run: |
          aws cloudformation deploy \
            --template-file infra/vpc-stack.yaml \
            --stack-name spr0222-vpc-stack \
            --no-fail-on-empty-changeset

      - name: Show CloudFormation failure events
        if: ${{ failure() }}
        run: |
          aws cloudformation describe-stack-events \
            --stack-name spr0222-vpc-stack \
            --query 'StackEvents[?ResourceStatus==`CREATE_FAILED` || ResourceStatus==`UPDATE_FAILED` || ResourceStatus==`DELETE_FAILED`].[Timestamp,LogicalResourceId,ResourceStatus,ResourceStatusReason]' \
            --output table || true